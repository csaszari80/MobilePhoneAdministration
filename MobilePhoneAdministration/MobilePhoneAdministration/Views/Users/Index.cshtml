@model IEnumerable<MobilePhoneAdministration.Models.User>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CPID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ADlogin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostPlace.CostCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CostPlace.CostName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hidden)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CPID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ADlogin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostPlace.CostCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CostPlace.CostName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hidden)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
